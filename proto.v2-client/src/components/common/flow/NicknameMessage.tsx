import colors from "@/styles/color";
import styled from "styled-components";

const NicknameMessage = styled.div`
  color: ${colors.black};
  font-size: 18px;
  font-weight: 600;
  letter-spacing: -0.36px;
  line-height: 100%;

  margin-bottom: 20px;
`;

export default NicknameMessage;
