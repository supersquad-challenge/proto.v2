import colors from "@/styles/color";
import styled from "styled-components";

const FlowSectionName = styled.div`
  color: ${colors.black};
  font-size: 24px;
  font-weight: 600;
  letter-spacing: -0.48px;

  margin-bottom: 40px;
`;

export default FlowSectionName;
